interface:
  display_name: "SSH Deploy"
  short_description: "Save SSH targets with a form and publish local builds."
  default_prompt: "Use $ssh-deploy to open the form for a new SSH deployment profile."
