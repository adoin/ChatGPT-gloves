interface:
  display_name: "SSH Deploy"
  short_description: "Remember SSH targets securely and publish local builds."
  default_prompt: "Use $ssh-deploy to publish this project with a saved SSH deployment profile."
